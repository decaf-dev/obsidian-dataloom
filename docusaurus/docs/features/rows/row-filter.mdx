# Filtering rows

Click `Filter` to open the filter menu. When you add a new rule, the body rows will be filtered by rows that only match the criteria for that rule.

The following types are supported currently for filtering: `Text`, `Tag`, `Multi-tag` and `Checkbox`

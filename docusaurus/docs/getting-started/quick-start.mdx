---
sidebar_position: 1
---

import pluginInstallationVideo from "../../static/video/plugin-installation.mov";
import enableSyncVideo from "../../static/video/enable-sync.mov";
import enableFileExtensionsVideo from "../../static/video/enable-file-extensions.mov";
import newLoomSidebar from "../../static/video/new-loom-sidebar.mov";
import newLoomFolder from "../../static/video/new-loom-folder.mov";
import newLoomCommandPalette from "../../static/video/new-loom-command-palette.mov";

# Quick start

## Installation

### Install the plugin

1. In Obsidian, open **Settings**
2. Go to **Community plugins**
3. Select **Browse**
4. Search for **DataLoom** by **Trey Wallis**
5. Select **Install**
6. Then select **Enable**

<video width="1280" controls>
	<source src={pluginInstallationVideo} type="video/mp4" />
	Your browser does not support the video tag.
</video>

### Configure Obsidian

DataLoom saves data in JSON format in `.loom` files.

#### Enable detection of loom files

By default, Obsidian doesn't display `.loom` files in the suggest menu that opens when you type double brackets `[[`. For Obsidian to do this, you must enable the detection of all file extensions.

1. In Obsidian, open **Settings**
2. Select **File & Links**
3. Toggle **Detect all file extensions**

<video width="1280" controls>
	<source src={enableFileExtensionsVideo} type="video/mp4" />
	Your browser does not support the video tag.
</video>

#### Enable syncing of loom files

By default, Obsidian doesn't sync `.loom` files between devices. For Obsidian to do this, you must enable syncing of all file extensions.

1. In Obsidian, open **Settings**
2. Select **Sync**
3. Choose and connect to a remote vault
4. In the remote vault settings, Toggle **Sync all other types**
5. Repeat these steps on all devices that you want to sync `.loom` files to

<video width="1280" controls>
	<source src={enableSyncVideo} type="video/mp4" />
	Your browser does not support the video tag.
</video>

## Create a new loom

There are 3 ways to create a new loom

-   Click on the table icon on the sidebar
-   Right-click on any folder and click **New loom**
-   Use the command palette and search for **Create loom**

<video width="1280" controls>
	<source src={newLoomSidebar} type="video/mp4" />
	Your browser does not support the video tag.
</video>

<video width="1280" controls>
	<source src={newLoomFolder} type="video/mp4" />
	Your browser does not support the video tag.
</video>

<video width="1280" controls>
	<source src={newLoomCommandPalette} type="video/mp4" />
	Your browser does not support the video tag.
</video>
